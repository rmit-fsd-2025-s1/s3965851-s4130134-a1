import React from "react";

// Header component that appears at the top of each page.
// Displays the name of the web system in a styled banner.
const Header = () => (
  <header className="bg-blue-700 text-white p-4 text-xl font-bold text-center">
    TeachTeam System
  </header>
);

export default Header;
